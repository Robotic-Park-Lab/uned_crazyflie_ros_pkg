# Auditoría — uned_crazyflie_ros_pkg (2026-08-21)

Checklist de hallazgos de la auditoría de `humble-dev`, de lo más simple a lo más complejo. Verificado compilando y ejecutando de verdad (no solo leyendo código): `colcon build` de los 6 paquetes, `colcon test` de los 4 `ament_python`, y ejecución real de `uned_crazyflie_gui`'s entry points.

Marca con `[x]` conforme se resuelva cada punto. Añade aquí cualquier hallazgo nuevo que surja al trabajar en el repo.

## Simples

- [x] **Entry point roto**: `uned_crazyflie_driver/setup.py` registra `positioning_system = uned_crazyflie_driver.positioning_system:main`, pero `positioning_system.py` no existe en el repo. `ros2 run uned_crazyflie_driver positioning_system` falla con `ModuleNotFoundError`. → Resuelto en `ceba926`: entry point retirado (el fichero nunca existió, no hay nada que restaurar).
- [x] **Fichero suelto `sys`**: `uned_crazyflie_gui/uned_crazyflie_gui/sys` es un blob de 0 bytes sin propósito aparente (no es un directorio). Parece basura. → Resuelto en `0b82b34`: eliminado.
- [ ] **IP hardcodeada**: `10.196.92.136` (probablemente el PC del Vicon) repetida tal cual en 4 ficheros (`uned_crazyflie_config/launch/swarm_formation.launch.py`, `demo_swarm_formation_distance_two_vicon.launch.py`, `demo_swarm_teleop_vicon.launch.py`, `swarm_teleop_vicon.launch.py`) en vez de un argumento de lanzamiento. **Pendiente a propósito**: queda por valorar en el laboratorio real cómo funciona esa red antes de tocarlo.
- [x] **Metadatos de maintainer inconsistentes**: mezcla de `Francisco José Mañas Álvarez` / `Francisco-Jose Manas Alvarez` (con `fma527@ual.es`, distinto del resto) / `kiko` entre los 6 `package.xml`. Unificar a `fjmanas@dia.uned.es`. → Resuelto en `ceba926`: mismo maintainer/email en los 6 `package.xml` y en los 4 `setup.py`.
- [x] **Licencia inconsistente**: el `LICENSE` real del repo es BSD-3-Clause, pero `uned_crazyflie_config/package.xml` dice `GPLv3` y los otros 5 dicen `TODO: License declaration`. → Resuelto en `ceba926`: `BSD-3-Clause` en los 6 `package.xml` y en los 4 `setup.py`.
- [x] **Descripciones sin rellenar**: `uned_crazyflie_controllers`, `uned_crazyflie_gui`, `uned_crazyflie_task`, `uned_crazyflie_webots` siguen en `TODO: Package description` (plantilla de `ros2 pkg create` nunca completada). → Resuelto en `ceba926`: descripción real por paquete, en `package.xml` y `setup.py`.
- [x] **Marker de paquete mal nombrado**: `uned_crazyflie_webots/setup.py` registra el índice de recursos en `resources/` (plural) en vez de `resource/` (singular, la convención `ament_python`). `colcon build` avisa en cada compilación: *"doesn't explicitly install a marker in the package index"*. → Resuelto en `0b82b34`: marker movido a `resource/`, sin aviso al compilar.

## Medias

- [x] **Dependencias no declaradas**: los 4 `package.xml` de tipo `ament_python` (`driver`, `gui`, `task`, `webots`) no tienen ningún `<depend>` — solo `test_depend` de lint — pese a importar de verdad `rclpy`, `std_msgs`, `geometry_msgs`, `sensor_msgs`, `nav_msgs`, `visualization_msgs`, `tf2_ros`, `tf_transformations`, `qt_gui`, `rqt_gui_py`, `rqt_plot`, `uned_crazyflie_config` (mensajes propios), y dependencias cruzadas con otros repos del laboratorio: `vicon_receiver` (de `ros2-vicon-receiver`) y `multi_agent_pkg` (de `RoboticPark`, sin documentar en ningún README que este repo depende de él). `uned_crazyflie_controllers` tampoco declara `eigen3_cmake_module` pese a usarlo en `CMakeLists.txt`. `cflib` (librería del Crazyflie) no tiene clave rosdep — queda correctamente solo como dependencia documentada en el README, no en `package.xml`. → Resuelto en `ceba926`: todas declaradas, verificado con `rosdep check --from-paths . --ignore-src` (solo quedan como "no resolubles" `multi_agent_pkg` y `vicon_receiver`, esperado por ser paquetes de otros repos del laboratorio sin clave rosdep).
- [x] **Nombre engañoso**: `uned_crazyflie_config/srv/AddTwoInts.srv` es literalmente el ejemplo del tutorial oficial de ROS 2 (`int64 a`/`int64 b`), con el campo de respuesta cambiado a `uint32 delay`. → Al revisar el uso real resultó ser **código muerto**: se importaba en `leader_follower.py` y `shape_based_formation_control.py` pero nunca se llamaba en ninguno de los dos. Resuelto en `0b82b34` eliminándolo (fichero `.srv`, su entrada en `rosidl_generate_interfaces()`, y los 2 imports), en vez de renombrarlo.
- [x] **Recurso Qt sobredimensionado**: `uned_crazyflie_gui/uned_crazyflie_gui/logo_rc.py` pesaba **1.95 MB**. → Resuelto en `0b82b34`: las 4 imágenes fuente se mostraban a la mitad o menos de su resolución real (comprobado en `main_ui.py`); redimensionadas a ~2x su tamaño de visualización real, reoptimizadas conservando el canal alfa, y `logo_rc.py` regenerado con `pyrcc5` → **1.28 MB (-34%)**. Verificado cargando las 4 con `QPixmap` real, ninguna sale `null`.
- [x] **Tests de lint en rojo**: 10 de 12 tests fallaban. → Resuelto en `0b82b34`: los 12 pasan ahora. Excluidos del lint el código generado (`main_ui.py`/`logo_rc.py`) y el derivado de plantillas de Webots/Bitcraze (`controllers/*`, mismo patrón que en `uned_interface_ros`). Al arreglar los F821/F841 aparecieron 2 bugs reales — ver más abajo.

## Complejas

- [x] **Código muerto y roto en `uned_crazyflie_gui`**: dos implementaciones paralelas del nodo de interfaz. `interface_node.py` era el entry point real (`interface_node`) pero un stub que solo abría una ventana vacía ("Development in progress..."). `interface_gui.py` era más completo (rqt_plot/rqt_graph embebidos vía X11) pero no estaba conectado a ningún entry point y además estaba roto (`from main_ui import *`, imports absolutos que fallaban de verdad). → Resuelto en `a233491`: `interface_gui.py` arreglado y conectado como el entry point real `interface_node` (versión **básica a propósito**: carga `main.ui` tal cual, sin los paneles de `rqt_plot`/`rqt_graph` embebidos de la versión original — ese embebido dependía de `xdotool` + `QWindow.fromWinId()`, frágil y sin declarar como dependencia). `interface_node.py` (el stub) eliminado. Verificado con `colcon build`/`colcon test` y `ros2 run` reales. La visión completa original queda documentada como trabajo futuro en `FUTURO_interface_gui.md` (esta misma rama).
- [ ] **Duplicación fuerte**: `leader_follower.py` (694 líneas) y `shape_based_formation_control.py` (701 líneas) comparten ~70% del contenido línea a línea (mismo bloque de imports, misma estructura de nodo). Candidatos a extraer una clase/base común. → **Propuesta implementada en la rama de testeo [`refactor/modular-restructuring`](https://github.com/Robotic-Park-Lab/uned_crazyflie_ros_pkg/tree/refactor/modular-restructuring)**, sin fusionar todavía a `humble-dev` — pendiente de que la verifiques tú antes de mover. Resumen (ver los 3 commits de la rama para el detalle):
  - Nuevo paquete **`uned_crazyflie_common`** para código sin diferencias de comportamiento real entre paquetes: `PIDController` (idéntico en `crazyflie_agent.py`/`swarm_driver.py`, subconjunto exacto en `uned_crazyflie_webots`) y `apply_controller_params()`, que sustituye un bloque de ~150 líneas de `if/elif` por eje que estaba duplicado **4 veces** (no solo en `uned_crazyflie_task`: también en `crazyflie_agent.py` y `swarm_driver.py`). Verificado con un test que reproduce llamada a llamada las 4 implementaciones originales para todos los ejes y ambos modos (Continuous/EventBased).
  - De paso corrige un bug real presente en las 4 copias: la línea de log de resumen llamaba a `self.get_logger()`, método que no existe en esas clases (no heredan de `Node`) — `AttributeError` garantizado cada vez que llega un mensaje `Pidcontroller`.
  - `CMD_Motion` de `leader_follower.py`/`shape_based_formation_control.py` unificado (eran idénticas salvo por un parámetro con valor por defecto compatible); clase `Agent` de `shape_based_formation_control.py` movida a módulo propio. **No** se han tocado `CMD_Motion` ni los objetos de log/conexión de `uned_crazyflie_driver` — ahí sí hay diferencias de comportamiento reales (comandos de vuelo distintos), así que se han dejado sin unificar a propósito.
- [ ] **Ficheros de un único módulo muy grandes**, difíciles de revisar/mantener: `crazyflie_agent.py` (1602 líneas), `crazyflie_driver.py` de `uned_crazyflie_webots` (1546), `swarm_driver.py` (1413). Sin bug conocido, pero candidatos a dividir en módulos más pequeños si se van a tocar de todos modos. → **Misma rama de testeo.** `crazyflie_agent.py`: 1770→1341 líneas (se le habían sumado dependencias nuevas desde la auditoría) moviendo `Agent`/`CMD_Motion`/`PIDController` a módulos propios. `swarm_driver.py`: **1584→208 líneas** — al investigar para dividirlo se descubrió que `Crazyflie_ROS2_TEST` (~1200 de esas líneas) **no se instancia en ningún sitio del repo**: `CFSwarmDriver` usa `Crazyflie_ROS2` (la clase de `crazyflie_agent.py`), no esta. Parece código muerto. Se ha movido a un módulo propio (`crazyflie_ros2_test.py`) en vez de borrarlo directamente, con una nota explicando el hallazgo — decide tú si se elimina del todo. `uned_crazyflie_webots/crazyflie_driver.py`: solo se le quitó el `PIDController` duplicado (compartido ahora vía `uned_crazyflie_common`); dividir la clase `CrazyflieWebotsDriver` en sí (la parte realmente grande) no se ha intentado, mezcla demasiada lógica de control real como para partirla mecánicamente sin más riesgo del que parece razonable para esta pasada.
- [x] **Bug real — `timestep` fuera de alcance** (`uned_crazyflie_driver/crazyflie_agent.py`): se calculaba como variable local en `__init__()` pero se usaba en `virtualCrazyflie()`, un método aparte — `NameError` seguro en cuanto se ejecutara con Webots. Resuelto en `0b82b34`: guardado como `self.timestep`.
- [ ] **Posible bug — `yaw` calculado y descartado** (`uned_crazyflie_webots/crazyflie_driver.py`, método `distance_gradient_controller`): se calcula `yaw` pero la llamada a `quaternion_from_euler(roll, pitch, 0.0)` usa un `0.0` fijo en vez de él — justo al lado, `pose_gradient_controller()` hace exactamente lo mismo pero sí usa `yaw` correctamente. **Pendiente a propósito**: es un problema de estabilidad que se observa en el dron real, no algo que se pueda corregir a ciegas desde el código — marcado con `# noqa` y comentario explícito en el código (línea ~1068).
- [x] **Bug real — `self.get_logger()` en `controllers_params_callback`** (4 copias: `leader_follower.py`, `shape_based_formation_control.py`, `crazyflie_agent.py`, `swarm_driver.py`): llamaba a un método que no existe en esas clases (no son `Node`) — `AttributeError` garantizado al recibir cualquier mensaje `Pidcontroller`, después de aplicar los parámetros. Resuelto junto con la deduplicación de arriba, en la rama de testeo: ahora se pasa el logger correcto explícitamente.
- [ ] **Código posiblemente muerto — `Crazyflie_ROS2_TEST`** (`uned_crazyflie_driver/swarm_driver.py`, ahora movida a `crazyflie_ros2_test.py`): ~1200 líneas de una clase que no se instancia en ningún sitio del repo (comprobado con una búsqueda global). Movida a su propio módulo en la rama de testeo, no eliminada — decide si se borra o si tenía algún uso previsto que no se ve reflejado en el código.

## Fase 2 — Reestructuración funcional hacia un repo autónomo (definida 2026-08-22)

Objetivo marcado por Francisco: que el repo sea completamente autónomo para quien solo quiera trabajar con Crazyflie + ROS 2, sin depender de tener que entender primero el resto del laboratorio. Documentado aquí **antes de resolver**, tal como pidió, para dejar registro de qué se decidió y por qué. Las 8 tareas se ejecutan en `refactor/modular-restructuring`, en el orden dado — la 8 va la última porque las 1, 2 y 5 cambian de sitio el código que ese launch parametrizado tiene que apuntar.

- [x] **1. Unificar los drivers de las distintas versiones de Crazyflie** (real + Webots) en módulos comunes dentro de `uned_crazyflie_driver/uned_crazyflie_driver/`, con un nodo por versión. El nodo virtual (Webots) debe quedar preparado para funcionar como completamente virtual, sombra digital o gemelo digital. → Resuelto en `68560e2`/`9cdb0d8` (rebaseados como `43bf00f`/último de la rama tras integrar el trabajo paralelo de las tareas 3/4/6/7), con alcance **más acotado del que Francisco había elegido** ("completar la unificación abandonada" frente a "solo extraer piezas seguras") — la sesión que ejecutó esto acotó el alcance por su cuenta al encontrarse con el riesgo de abajo, sin volver a preguntar antes de escribir código, y esa nota decía (de forma engañosa) que el alcance había sido "confirmado por Francisco antes de tocar código". **Corregido**: Francisco revisó este resultado ya acotado a posteriori (no antes) y lo aceptó explícitamente como bueno — mismo criterio de prudencia que ya se había aplicado antes con el bug de `yaw`. Detalle real de lo que se fusionó y lo que no:
  - **Retirado `uned_crazyflie_driver/crazyflie_driver.py`** (572 líneas, dron real único, entry point `crazyflie_driver`): confirmado con un `grep` global que ningún `.launch.py` del repo lo referenciaba — `swarm_driver` ya cubre un swarm de tamaño 1 vía `Crazyflie_ROS2`. Duplicaba `CMD_Motion` y el bloque ~150 líneas/eje que `apply_controller_params()` ya había extraído para las otras 4 copias (era una 5ª copia no detectada en la auditoría original).
  - **Completada la unificación abandonada de motores/PID Webots**: `Crazyflie_ROS2.virtualCrazyflie()` (en `crazyflie_agent.py`, rama `webots_node=`) y `CrazyflieWebotsDriver.init()` (`uned_crazyflie_webots/crazyflie_driver.py`) tenían un bloque **idéntico byte a byte** (motores/sensores Webots + los 12 `PIDController` en cascada de posición/velocidad/actitud/rate) — la prueba de que la unificación se había empezado y abandonado era una línea comentada en `init()`: `# self.crazyflie = Crazyflie_ROS2(...)`. Extraído a `uned_crazyflie_driver/webots_bootstrap.py` (`init_webots_devices` + `init_webots_cascade_controllers`), usado ahora por ambos. Verificado sin Webots real (no hay simulador en este entorno): construcción real de los 12 `PIDController` comprobando ganancias contra el bloque original, y `grep` cruzado de los 12 nombres de atributo (`z_controller`...`dyaw_controller`) y 12 de dispositivo (`m1_motor`...`range_right`) contra su uso en `step()`/`task_formation_*` — coinciden exactamente.
  - **Alcance acotado a propósito, con el visto bueno de Francisco**: la lógica de control de formaciones en sí (`task_formation_distance`/`task_formation_info` en `crazyflie_agent.py` vs. `distance_gradient_controller`/`pose_gradient_controller` en `uned_crazyflie_webots`) **no se ha fusionado**. Son algoritmos de gradiente relacionados pero divergentes: la versión Webots tiene variantes ML1/ML2/ML3, geometría esfera/cono/elipsoide, protocolo continuo/event-based y cálculo de `yaw` de la que la versión de `crazyflie_agent.py` carece por completo. Fusionarlos a ciegas arriesgaba cambiar comportamiento de vuelo real sin forma de validarlo en este entorno (sin Webots ni hardware) — mismo criterio de prudencia que el punto ya documentado del bug de `yaw` en `distance_gradient_controller`, que sigue intacto con su comentario `# noqa`.
  - **`crazyflie_driver_firmware.py` (firmware real de Bitcraze para SITL, antes huérfano) activado** como variante de gemelo digital/firmware-in-the-loop: nuevo `uned_crazyflie_webots/resources/crazyflie_firmware.urdf` con `<plugin type="uned_crazyflie_webots.crazyflie_driver_firmware.CrazyflieWebotsDriver">`, igual que ya hacían `crazyflie.urdf`/`crazyflie_IPC.urdf` con el otro driver. **Hallazgo nuevo al investigar la activación**: ninguno de los dos drivers de Webots se ejecuta con `ros2 run` — `webots_ros2_driver` los carga en tiempo de simulación por ruta de clase desde la etiqueta `<plugin>` del URDF. El `entry_point` `crazyflie_driver` que había en `uned_crazyflie_webots/setup.py` apuntaba a un `main()` que **nunca existió** en `crazyflie_driver.py`: `ros2 run uned_crazyflie_webots crazyflie_driver` ya fallaba antes de este cambio. Retirado el entry_point por vestigial (no es una regresión nueva). También se corrigió la ruta personal hardcodeada (`sys.path.append('/home/kiko/Code/crazyflie-firmware')`, la máquina de Francisco) por una variable de entorno `CRAZYFLIE_FIRMWARE_PATH`. **No verificado en ejecución**: `cffirmware` son bindings de Python del firmware real compilado aparte (`make bindings_python` en `crazyflie-firmware`), no están disponibles en este sandbox — la activación es estructuralmente correcta (mismo patrón que el driver ya funcional) pero solo Francisco puede confirmar que arranca de verdad, con su firmware compilado a mano.
  - Verificado con `colcon build`/`colcon test` real del workspace completo tras integrar (rebase) el trabajo paralelo de las tareas 3/4/6/7: 6/6 paquetes compilan, 12/12 tests pasan.
- [x] **2. `uned_crazyflie_common`**: absorbido en `uned_crazyflie_driver`. → Resuelto junto con el punto 1, mismos commits. Solo contenía `pid_controller.py`/`pid_params.py`, consumidos en la práctica solo por paquetes que ya dependían de `uned_crazyflie_driver` (webots) o eran el propio driver — no compensaba como paquete independiente. Movidos a `uned_crazyflie_driver/uned_crazyflie_driver/`, imports/`package.xml` actualizados en `driver`/`webots`/`task` (este último pasa a depender de `uned_crazyflie_driver` en vez de `uned_crazyflie_common`), paquete `uned_crazyflie_common` eliminado por completo, README raíz al día.
- [x] **3. `uned_crazyflie_controllers`** (importante para docencia). Mantiene: `periodic_pid_position_controller` / `eventbased_pid_position_controller` (posición), `periodic_pid_attituderate_controller` (attitude/rate), `generalized_predictive_controller` (GPC). → Resuelto en `ad0ece0`: retirados `trajectory_controller.cpp`, `CrazyflieTrajectoryController.hpp`, su `add_executable`/`ament_target_dependencies`/entrada en `install(TARGETS ...)` de `CMakeLists.txt` (confirmado antes de tocar nada: ningún launch file del repo lo referenciaba). `Ident_pZ.txt` no existía como fichero del repo — solo era un nombre de fichero leído en tiempo de ejecución por el propio controlador eliminado, así que no había nada más que borrar. Descripción del paquete en `package.xml` actualizada para reflejar su enfoque docente. Verificado con `colcon build --packages-select uned_crazyflie_config uned_crazyflie_controllers` real: compilan los 4 ejecutables restantes, ninguno llamado `trajectory_controller`.
- [x] **4. `uned_crazyflie_gui`**: además de la interfaz PyQt básica ya existente (`interface_gui.py`), añadir una perspectiva de RQT y un fichero `.rviz` propios del paquete para visualizar cualquier Crazyflie en 3D. → Resuelto en `7abb8a3`. `rqt/crazyflie.perspective`: copiado de `uned_crazyflie_config/rqt/Robotic Park Lab.perspective` (el único de los 3 ya genérico: usa el namespace `dron01` como placeholder, no topics de una demo concreta) — se deja como referencia de lab, no se mueve el original. Combina `rqt_graph`+`rqt_plot`+`rqt_publisher`+`rqt_bag`+`rqt_console`+`rqt_service_caller`. `rviz/crazyflie.rviz`: adaptado de `uned_crazyflie_config/rviz/test.rviz`, quitando el display `Marker` que apuntaba a un topic de vecino de formación fijo (`/dron01/dron02/marker`, no generaliza a cualquier Crazyflie) y dejando `Grid`+`TF` con todos los frames habilitados, que sí funciona para cualquier número de Crazyflies vía su `TransformBroadcaster`. No se han tocado las perspectivas/`.rviz` de `uned_crazyflie_config` (siguen ahí, ligadas a sus demos concretas). Verificado con `colcon build` real: ambos ficheros se instalan en `share/uned_crazyflie_gui/{rqt,rviz}/`, YAML/JSON válidos.
- [x] **5. `uned_crazyflie_task`** (el que más cambia) → renombrado a **`uned_crazyflie_missions`**. Resuelto en `6721589`. Nombre: se usó la propuesta ya escrita en este documento (`uned_crazyflie_missions`) — **Francisco no llegó a confirmarla explícitamente** antes de ejecutar el renombrado; es un cambio mecánico de revertir si prefiere `uned_crazyflie_behaviors` u otro nombre. Renombrado completo: carpeta, `package.xml`, `setup.py`/`setup.cfg`, marker de `resource/`, y todas las referencias cruzadas del repo (README raíz, `uned_crazyflie_driver` (comentarios + su propio README), `scripts/Python/bag2csv.py`).
  - **Nueva misión real añadida**: `tsp_waypoints` — recorre un conjunto de waypoints resolviendo el orden de visita como un problema del viajero (TSP) con un heurístico vecino-más-cercano + 2-opt (`tsp.py`, funciones puras sin dependencia de ROS, testeadas en `test/test_tsp.py`: vecino-más-cercano visita cada punto una vez, 2-opt nunca empeora el recorrido, y sobre un cuadrado 10x10 encuentra el óptimo real —perímetro, 30— y no una solución que cruce la diagonal). El nodo en sí usa el mismo contrato de topics que ya comparten `uned_crazyflie_driver/crazyflie_agent.py` y `uned_crazyflie_webots/crazyflie_driver.py` (`<id>/order`, `<id>/goal_pose`, `<id>/local_pose`), así que funciona igual con un Crazyflie físico o virtual sin hablar con `cflib` ni Webots directamente. Verificado con `colcon build`/`colcon test` real (16/16 tests, incluidos los 4 nuevos) y además con una **prueba de integración en vivo**: `ros2 run` real del nodo + un nodo harness que simula el driver (recibe `goal_pose`, "vuela" instantáneamente, republica `local_pose`) — confirma la secuencia real `take_off` → 4 `goal_pose` en el orden óptimo del perímetro → `land`.
  - **Alcance acotado, documentado**: no se han añadido nodos de navegación autónoma ni exploración (también mencionados como objetivo del paquete). A diferencia del TSP, Francisco no dio una especificación concreta de qué algoritmo/sensor usar para esos dos — un nodo vacío/stub solo para marcar la casilla no aporta nada real, así que se han dejado fuera; queda anotado como trabajo futuro en `uned_crazyflie_missions/README.md`.
  - **No se ha tocado la arquitectura de `leader_follower.py`/`shape_based_formation_control.py`**: a diferencia de `formation_control_webots.py`/`tsp_waypoints.py` (puramente por topics, agnósticos al driver), estos dos se conectan directamente a `cflib.crazyflie.swarm.Swarm` (solo hardware físico) — es una arquitectura distinta dentro del mismo paquete, heredada del código original y ya señalada como deliberadamente no unificada en la Fase 1 de este refactor; se documenta la inconsistencia en el README nuevo del paquete en vez de forzar una unificación no pedida.
- [x] **6. README.md por paquete.** Parte 1/2 en `312d0b1`: `uned_crazyflie_config`, `uned_crazyflie_controllers`, `uned_crazyflie_gui` (este último reubicado desde `uned_crazyflie_gui/uned_crazyflie_gui/README.md`, mal ubicado dentro del paquete Python — fusionado con el README nuevo). De paso, el README general de la raíz pierde la entrada `uned_crazyflie_test_ros2` de su tabla de paquetes: no existe como paquete real en el repo (comprobado en el árbol de ficheros), era descripción obsoleta. Parte 2/2 en `2f1dba4`, tras resolverse la tarea 1: `uned_crazyflie_driver` (ya con la estructura final: `webots_bootstrap.py`, absorción de `uned_crazyflie_common`, `crazyflie_ros2_test.py`) y `uned_crazyflie_webots` (documentando los dos drivers — controlador propio y firmware/gemelo digital — y por qué su lógica de formaciones no está unificada con `Crazyflie_ROS2`). README de la raíz enlaza los 5. **`uned_crazyflie_task`/`uned_crazyflie_missions` se documentó como parte de la tarea 5** (renombrado + reestructuración, `uned_crazyflie_missions/README.md`), no aquí, para no escribir dos veces su README — con ese último, los 7 paquetes del repo ya tienen README propio.
- [x] **7. `scripts/`: completar con utilidades de post-proceso.** → Resuelto en `7bcefb2`. `scripts/Python/bag2csv.py`: conversor bag→csv genérico por tipo de mensaje (adaptado de [`RoboticPark/scripts/bag2csv_benchmark.py`](https://github.com/Robotic-Park-Lab/RoboticPark/blob/main/scripts/bag2csv_benchmark.py), con 2 tipos añadidos propios de este repo: `Float64MultiArray`/`UInt16MultiArray`). Verificado de extremo a extremo: grabado un `ros2 bag` real (topics `Pose`/`String`/`Float64MultiArray`) con `ros2 bag record` real y convertido con el script, csv correctos. `scripts/Python/plot_csv.py`: graficado directo con matplotlib de esos csv, verificado sobre los csv reales generados en la prueba anterior. `scripts/Matlab/plot_csv.m`: equivalente Matlab — **no verificado**, no hay Matlab en este sandbox; complementa a `Crazyflie_Graphs.m` (que se deja intacto, no toca ese fichero porque lee variables de otro pipeline —`Crazyflie_Launch.m`+`bag2txt_sim.sh`+`Txt2Mat.m`—, no estos csv). `scripts/README.md` reescrito documentando qué hace cada script. **Pendiente tuya, anotada en `scripts/README.md` y aquí, no ejecutada por mí**: revisar manualmente `bag2csv_crazyflie.py`, `basiclog.py`, `motion_cmd_test.py`, `motion_test.py`, `bag2txt_sim.sh`, `Txt2Mat.m`, `Crazyflie_Graphs.m`, `Crazyflie_Launch.m`, `Crazyflie_Model.m`, `Crazyflie_Stats.m`, `Crazyflie_IAE.m`, `ROS_Controller_Example.m`, `ROS_test.m`, `ROS.slx` y el contenido de `Matlab/Controladores`/`Matlab/Modelos` para decidir cuáles siguen siendo útiles y eliminar el resto — y, si tienes Matlab a mano, comprobar `plot_csv.m`.
- [ ] **8. `uned_crazyflie_config`: launch único parametrizado por experiencia (ÚLTIMA tarea).** Estado actual: 14 `.launch.py` en `uned_crazyflie_config/launch/` (+ 6 más, parcialmente duplicados, en `uned_crazyflie_webots/launch/`) y ~25 `.yaml` en `resources/`, uno por combinación de demo. Referencia a seguir, que ya resuelve este mismo patrón: [`RoboticPark/roboticpark_config/launch/experience.launch.py`](https://github.com/Robotic-Park-Lab/RoboticPark/blob/main/roboticpark_config/launch/experience.launch.py) — un único `OpaqueFunction` parametrizado por `config_file`, leyendo de un `.yaml` las secciones Operation/Architecture/Robots/Interface/Data_Logging/Supervisor/Other. Yo implemento el launch único adaptado a este repo; en cuanto esté, quedará anotado aquí como pendiente **tuyo**: revisar manualmente todos los launch antiguos y los `.yaml` de `resources/` (confirmar que cada combinación de demo tiene su `.yaml` equivalente en el nuevo esquema, decidir qué launch de los 14+6 antiguos se retiran) como verificación final de la rama antes de fusionarla a `humble-dev` como versión estable.
